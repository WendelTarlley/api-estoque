
+CREATE TABLE item_preco (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   preco DECIMAL,
   data_cadastro TIMESTAMP WITHOUT TIME ZONE,
   user_id UUID,
   item_id BIGINT,
   CONSTRAINT pk_itempreco PRIMARY KEY (id)
);

ALTER TABLE item_preco ADD CONSTRAINT FK_ITEMPRECO_ON_ITEM FOREIGN KEY (item_id) REFERENCES itens (id);

ALTER TABLE item_preco ADD CONSTRAINT FK_ITEMPRECO_ON_USER FOREIGN KEY (user_id) REFERENCES users (id);